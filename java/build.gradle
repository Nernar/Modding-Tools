apply plugin: 'com.android.application'

android {
    defaultConfig {
        applicationId "com.nernar.editor"
        minSdkVersion 19
    }
}

dependencies {
    implementation fileTree(dir: "lib", include: ["*.jar"])
}